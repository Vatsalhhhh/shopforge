[tool.ruff]
line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "PT", "RET", "SIM"]
ignore = ["E501", "B008", "N818"]

[lint.per-file-ignores]
"tests/*" = ["S101"]   # allow assert in tests
"alembic/*" = ["F401"]
